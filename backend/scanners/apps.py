from django.apps import AppConfig

class ScannersConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'scanners'
    label = 'scanners'
    verbose_name = 'Módulo de Escaneo e Inferencia de Imágenes'
